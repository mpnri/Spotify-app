import settings from '../../assets/icons/settings.svg';

const Toolbar = () => `
    <div class="toolbar">
        <img src="${settings}" alt="settings" class="toolbar__settings">
    </div>
`;

export default Toolbar;